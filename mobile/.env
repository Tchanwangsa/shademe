# The phone cannot reach the API on localhost -- this must be the Mac's LAN address,
# and uvicorn must be started with --host 0.0.0.0 to accept it. Re-run
# `ipconfig getifaddr en0` and update this after switching networks.
EXPO_PUBLIC_API_BASE=http://100.86.185.85:8011
